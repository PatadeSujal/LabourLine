spring:
  application:
    name: LabourLine
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/labourline_db}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:Sujal@2006}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
